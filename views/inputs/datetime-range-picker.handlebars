<div class="form-group input-datetimerange col-md-{{width}}">
    <label>{{title}}</label>
    <div class="input-group">
        <input name="{{key}}[from]" value="{{value.from}}" type="text" class="form-control">
        <span class="input-group-addon">to</span>
        <input name="{{key}}[to]" value="{{value.to}}" type="text" class="form-control">
    </div>
</div>
