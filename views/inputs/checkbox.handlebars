<div class="form-group col-md-{{width}}">
    <label>{{title}}</label>
    {{#each options}}
        <div class="checkbox">
            <label>
                {{#in value ../value}}
                <input name="{{../key}}" type="checkbox" value="{{value}}" checked>{{name}}
                {{else}}
                <input name="{{../key}}" type="checkbox" value="{{value}}">{{name}}
                {{/in}}
            </label>
        </div>
    {{/each}}
</div>
